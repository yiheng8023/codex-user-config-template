# Hooks

This directory is a policy placeholder.

Do not place live Hook automation here in the public template. Hook examples must be safe, inert, and clearly marked as examples.

Hooks can remind or inspect, but they must not silently snapshot memory, restore state, commit, push, install, or mutate runtime configuration without explicit user authorization.
