# Example only. Copy to a private repository before adding real values.

[profile]
name = "example-user"
language = "en"

[repositories]
# Optional governance input. Leave empty unless a reviewed source is bound.
skill_policy_source = "https://github.com/example/reviewed-skill-policy"

[safety]
public_template = true
contains_real_memory = false
contains_credentials = false
