# Minimal valid Codex config.toml example.
# Copy it to a private repository before adding personal values.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false
